<h1>ジャンル名変更</h1>

<h4>現在のジャンル名</h4>
<%= @genre.name %>
<%= link_to "変更せずに戻る", genre_path(@genre) %>

<%= form_with model: @genre do |f| %>
  
  <h4>新しいジャンル名</h4>
  <%= f.text_field :name %>
  
  <%= f.submit '更新する' %>
  
<% end %>
