<h1><%= @genre.name %></h1>

<%= link_to "ジャンル名を変更する", edit_genre_path(@genre.id) %>

<%= link_to "このジャンルを削除する", genre_path(@genre.id), method: :delete, 
"data-confirm" => "ジャンルを削除するとジャンル内の全ての食材のデータも削除されます。本当によろしいですか？" %>
    
    <table class="table table-hover table-bordered table-condensed">
      <thead>
		    <tr>
		      <th>食材</th>
	      	  <th>購入日</th>
		      <th>賞味期限</th>
		      <th>数量</th>
		      <th>メモ</th>
		      <th></th>
		      <th></th>
		    </tr>
      </thead>
      <tbody>
        <% @ingredients.each do |ingredient| %>
            <tr>
              <td><%= ingredient.name %></td>
              <td><%= ingredient.purchase_date %></td>
              <td><%= ingredient.expiration_date %></td>
              <td><%#= ingredient.quantity %><%= number_with_precision(ingredient.quantity, precision: 1) %><%= ingredient.unit %></td>
              <td><%= form_with model: [@ingredient, @comment] do |f| %>
                  <%= f.text_area :comment, rows: '2', placeholder: "メモ" %>
                  <%= f.submit "メモを保存する" %>
                  <% end %>
              </td>
              <td><%= link_to "編集する", edit_ingredient_path(ingredient.id) %></td>
              <td><%= link_to "削除する", ingredient_path(ingredient.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
            </tr>
           <% end %>
	    </tbody>
    </table>

